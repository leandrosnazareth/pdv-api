# =================================
# = Spring conectar com db testcontainers do docker
# =================================
spring:
    datasource:
        driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
        url: jdbc:tc:mysql://localhost/pdvdb
        username: admin
        password: admin
    main:
        allow-bean-definition-overriding: true